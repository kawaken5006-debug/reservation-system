import React from 'react';
import ReactDOM from 'react-dom/client';
import ReservationSheet from './reservation_modified';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ReservationSheet />
  </React.StrictMode>
);
